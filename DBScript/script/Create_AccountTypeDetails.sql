use PackageDB

go

--this use for showing the data is income or expense
--this use for showing the data is income or expense
create table AccountTypeDetails
(
	AccountTypeId varchar(10) primary key,
	AccountType varchar(50)
)

go