use PackageDB

go

--this table use for showing the detail of account, the data from app icon
create table AccountItermDetails
(
	AccountItermId varchar(10) primary key,
	AccountIterm varchar(50)
)

go